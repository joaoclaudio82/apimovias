services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: movias_api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
